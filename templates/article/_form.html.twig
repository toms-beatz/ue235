{{ form_start(form, {'attr': {'class': 'w-50 align-self-center article-form'}}) }}

    {{ form_label(form.title, 'Titre') }}
    {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}

    {{ form_label(form.title, 'Contenu') }}
    {{ form_widget(form.content, {'attr': {'class': 'form-control'}}) }}

    {{ form_label(form.title, 'Catégorie') }}
    {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}

    <button class="save btn btn-success align-self-start">{{ button_label|default('Enregistrer') }}</button>
    <button class="back btn btn-primary align-self-start"><a href="{{ path('article_index') }}">Retourner aux articles</a></button>

{{ form_end(form) }}

